# e2e_calib runtime requirements (Python 3.13 + CUDA 13).
# Snapshot from yokohama's working env (2026-05-14).
# Pinned where breakage observed; ranged where minor versions don't matter.

# Core
torch==2.11.0
numpy==2.2.6
scipy==1.16.1
pyyaml==6.0.2

# Data + decoding
pillow==12.2.0
opencv-python==4.12.0.88
pyturbojpeg==1.8.3
pyarrow==22.0.0
lmdb==2.2.0

# Dataset SDKs
# nuscenes-devkit installed --no-deps in Dockerfile (its `numpy<2.0` pin conflicts
# with the rest of the stack; runtime API works fine with numpy 2.x).
av2==0.3.1
zod==0.8.0

# Vis / plotting
matplotlib==3.10.6
plotly==6.3.0
netron==9.0.0

# Math / opt
pyceres==2.6

# ClearML
clearml==2.1.6

# Server stack (used by scripts/serving/)
fastapi==0.135.3
starlette==0.52.1
uvicorn==0.37.0
pydantic==2.12.5

# Misc training deps
accelerate==1.10.1
pandas==2.3.2
aiohttp==3.13.5
orjson==3.11.3
markdown==3.8.2
boto3==1.40.42
