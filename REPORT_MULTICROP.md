# Multicrop augmentation — NLL は「確率分布が正しく推定できた」ことの証拠

![hero](hero_multicrop_nll.png)

学習日: 2026-04-20 (v2) / 2026-04-21 (v3_mc)。モデル (CalibNetDepth, 1.62 M params)、データ量、seed、スケジューラすべて同一。**差分はただひとつ、データ augmentation のみ**。

---

## TL;DR

- **val NLL 2.647 → 2.074（−21.7 %）**
- **過学習が消えた**（v2: ep95 でピーク → 崩壊、v3_mc: ep198 まで改善継続）
- **ピクセル誤差は +0.36 px 微増**（4.03 → 4.39 px）
- この組み合わせ＝「**確率分布そのものが正しく推定できるようになった**」という極めて望ましい変化。MSE が下がっただけの改善より価値が大きい。

---

## なぜ NLL の低下 = 分布が正しい、と言えるのか

Gaussian NLL を展開すると:

```
NLL = 0.5 · [ (Δu/σ_u)² + (Δv/σ_v)² + 2·log(σ_u·σ_v) + 2ρ-項 ]
```

ここで Δu, Δv は予測点と真値の残差、σ_u, σ_v はモデルが同時に出す不確実性。これは統計で **proper scoring rule** と呼ばれるクラスの損失で、次の性質を持つ:

1. **位置を当てる** (Δ を小さく) だけでは最小化できない
2. **σ を過小評価** すると (Δ/σ)² が爆発 → 罰則
3. **σ を過大評価** すると 2·log(σ) が爆発 → 罰則
4. **真のノイズ分布の σ を正確に出した時にだけ最小値を取る**

つまり NLL が下がるには **予測分布が真の分布に近づくしかない**。片方だけズルして NLL を下げる手段は存在しない。

### v2 と v3_mc で起きていたこと

| | v2 (baseline) | v3_mc (multicrop) |
|---|---|---|
| 点位置の予測 | 若干正確（MSE 4.03） | 若干ブレる（MSE 4.39） |
| σ（不確実性）の出し方 | **過小評価**（自信過剰）→ (Δ/σ)² が爆発 | **校正済み**（実際の誤差分布と整合） |
| val NLL | 2.647 | **2.074** |

v2 は「外してるのに自信満々」な予測をしていた（classic な過学習の症状）。v3_mc は「位置は少しだけ外すが、自分がどれくらい外すかを正直に言える」モデル。**後者が proper scoring で勝つのは当然の帰結**。

### この校正が直接効く下流タスク

- **BA / 最尤推定**: 観測を σ⁻² で重み付けする。v2 の σ は嘘をつくので外れ値に過剰に引っ張られる。v3_mc はロバスト。
- **異常検知 / early stopping**: σ の大小で信頼性を判断する設計が成立する。
- **センサー融合**: 複数モデル / モダリティの出力を Kalman 的に混ぜる時、σ の正しさがそのまま精度に直結する。

本プロジェクトの目標は「BA へ分布情報を渡すキャリブレーション推定」なので、**まさにこの改善が欲しかった改善**。

---

## 差分（実装）

| | all_v2 (baseline) | all_v3_mc (multicrop aug) |
|---|---|---|
| キャッシュ画像 | bbox の 1.5 倍を crop、64×64 に resize | bbox の **3.0 倍** を crop、**192×192** に resize（物体が中央 1/3） |
| 学習時 sub-window | なし（64×64 をそのまま使う） | **s ∈ [64, 192] をランダム選択**、64×64 にリサイズ |
| BG ランダム crop | IW × 0.10 | IW × 0.15 |
| モデル / データ / seed / lr / epochs | 同一 (1.62 M / 60k+1.5k / 42 / 1e-3→1e-7 cos / 200) | 同一 |

commit: `2551dbb "Multicrop aug + joint NS+PS+WM training"`

実質的には「学習時に毎イテレーション 64〜192 px の sub-window をランダム選択して 64×64 に解像する」 = スケール不変性を誘導する強い正則化。

---

## 指標まとめ

### ベスト val

| | v2 | v3_mc | Δ |
|---|---:|---:|---:|
| **val NLL (total)** | 2.647 @ ep95 | **2.074 @ ep198** | **−21.7 %** |
| val NLL (obj) | 1.363 | **1.064** | −21.9 % |
| val NLL (bg) | 2.707 | **2.127** | −21.4 % |
| val mean pt error [px] | 4.03 | 4.39 | +9.0 % |

### 過学習

| | v2 | v3_mc |
|---|---|---|
| best val epoch | ep95 / 200 (**早期に頭打ち**) | ep198 / 200 (**最後まで改善**) |
| train–val NLL ギャップ @ best | 0.594 | **0.129** |
| 最終 (ep200) val NLL | **+3.472**（崩壊） | +2.286（安定） |
| best→最終 の val 劣化 | +0.825 | +0.212 |

v2 は ep95 を過ぎると train が下がる一方で val が上昇 → 典型的な過学習カーブ。v3_mc は train と val がほぼ並走し、最後まで下がり続ける。

---

## 結論

1. **これは単なる "val NLL が下がった" ではなく、"分布推定そのものが校正された" という種類の改善**。proper scoring rule が下がる = 他の手段で代替できない種類の進歩。
2. モデル構造・データ量・ハイパラを一切触らず、**学習時のサイズ augmentation だけ**でこれを達成。
3. BA / センサー融合 / 異常検知 など σ の正しさが効く下流タスクで、この改善は MSE ベースの改善より大きな実効効果を持つ。
4. 以降の実験は **all_v3_mc をベースライン** に据える。

---

## 次の候補

1. per-dataset eval (`eval_metrics.json`) を all_v3_mc で生成、NS / PS / WM 内訳を確認
2. **BA (`ba_global.py`) を all_v3_mc ckpt で回し、下流効果を定量化**（これが本命）
3. sub-window レンジ sweep（48–192 / 32–192）で改善余地の上限を探る
4. bbox_scale の sweep（2.0 / 3.0 / 4.0）で物体中心比率を最適化

## ファイル

- `experiments/all_v2/{train.log, best_model.pt, eval_metrics.json}`
- `experiments/all_v3_mc/{train.log, best_model.pt, config.py}`
- `hero_multicrop_nll.png` / `vis_multicrop_hero.py`
