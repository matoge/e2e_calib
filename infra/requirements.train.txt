# infra/requirements.train.txt
#
# nvcr.io/nvidia/pytorch:24.02-py3 に追加でインストールする python パッケージ。
# base image にすでに以下が入っているので重複は入れない:
#   torch, torchvision, numpy, scipy, pillow, opencv-python(headless), tqdm
#
# ここでは DDP + ClearML + 自前コードが直接 import するものだけを pin する。

accelerate==0.34.2
clearml==1.16.4
einops==0.8.0
pyyaml>=6.0
# numpy>=2.0 必須: waymo_v3_tiled / pandaset_v3_full cache の inst/*.pt は
# numpy 2.x で pickle されており (np.ndarray の内部 module が `numpy._core`)、
# numpy 1.x 環境で torch.load すると ModuleNotFoundError: numpy._core で死ぬ。
# nvcr.io/nvidia/pytorch:24.02 (torch 2.3.0a0) は numpy 2.x と binary compat。
numpy>=2.0,<3.0
# dataset 側で使っている: pandaset v3 (fsx cache) / waymo v3 cache は pt を直読。
# ただし V3 tile cache は JPEG bytes を .pt 内に保持しており、__getitem__ 側で
# PyTurboJPEG で decode する。base image には無いので明示的に入れる。
# NOTE: PyPI 名前衝突注意 — `turbojpeg` (Dobatymo/turbojpeg-python) は
# `TurboJPEG` class を持たず、pandaset_full.py の optional-import が silent
# fallback で PIL 経路に落ちる。必ず `PyTurboJPEG` (Lilohuang/PyTurboJPEG) を
# pin する。詳細は docs/sps_bottleneck_analysis.md §8.1。
PyTurboJPEG==1.7.5
# 可視化系 (matplotlib等) は base image 標準に任せる。 eval/render 時だけ使うため学習 image に
# は含めないポリシー (将来必要なら infra/Dockerfile.eval を別立てする)。
