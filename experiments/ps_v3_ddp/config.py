CFG = dict(
    name         = 'ps_v3_ddp',
    n_layers     = 4,
    img_size     = 256,
    in_channels  = 3,
    use_convnext = True,
    use_frustum  = True,
    epochs       = 50,
    batch_size   = 32,
    lr           = 0.001,
    lr_min       = 1e-06,
    val_fraction = 0.1,
    split_seed   = 42,
    min_crop_px  = 128,
    max_crop_px  = 512,
    cache        = '/home/hfunaya/cache_v4/kamikado_v3_tiled',
    max_rot_deg  = 1.5,
    max_offset_m = 0.2,
    oversample   = 16,
    grid_n       = 32,
    num_workers  = 8,
    deform_mode  = 'sl',
    use_pose_emb = True,
    ba_eval_start_ep= 1,
    ba_eval_n_seeds= 4,
    ba_eval_n_inst= 20,
)
