CFG = dict(
    name         = 'ps_tile_clspose_smoke',
    n_layers     = 4,
    img_size     = 128,
    in_channels  = 3,
    use_convnext = True,
    use_frustum  = True,
    epochs       = 2,
    batch_size   = 128,
    lr           = 0.0003,
    lr_min       = 1e-07,
    val_fraction = 0.1,
    split_seed   = 42,
    min_crop_px  = 128,
    max_crop_px  = 384,
    cache        = '/mnt/nvme6t/e2e_calib_cache/pandaset_v3_tiled_multicam_corr',
    max_rot_deg  = 1.5,
    max_offset_m = 0.6,
    oversample   = 1,
    num_workers  = 12,
    deform_mode  = 'sl',
    use_frame_pose= True,
    frame_pose_weight= 0.5,
    val_size     = 2000,
)
