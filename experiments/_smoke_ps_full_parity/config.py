CFG = dict(
    name         = '_smoke_ps_full_parity',
    n_layers     = 4,
    img_size     = 128,
    in_channels  = 3,
    use_convnext = True,
    use_frustum  = True,
    epochs       = 1,
    batch_size   = 8,
    lr           = 0.001,
    lr_min       = 1e-06,
    val_fraction = 0.1,
    split_seed   = 42,
    min_crop_px  = 256,
    max_crop_px  = 512,
    cache        = '/mnt/fsx/tmp/hfunaya/e2e_calib_cache/pandaset_v3_full',
    max_rot_deg  = 1.5,
    max_offset_m = 0.2,
    oversample   = 1,
    grid_n       = 16,
    num_workers  = 4,
    deform_mode  = 'ml',
    train_size   = 64,
    val_size     = 16,
    use_pose_emb = True,
    frustum_dense= True,
)
