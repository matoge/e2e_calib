CFG = dict(
    name         = 'v600_nuscenes_only',
    n_layers     = 4,
    img_size     = 64,
    in_channels  = 3,
    use_convnext = True,
    use_frustum  = True,
    epochs       = 50,
    batch_size   = 64,
    lr           = 0.001,
    lr_min       = 1e-07,
    val_fraction = 0.1,
    split_seed   = 42,
    min_crop_px  = 128,
    max_crop_px  = 512,
    cache        = '/mnt/nvme6t/e2e_calib_cache/nuscenes_v3_full',
    frame_stride = 5,
    grid_n       = 16,
    num_workers  = 24,
    val_size     = 3000,
)
