# auto-generated by train_cnd2_ddp.py at run start
CFG = dict(
    name                = 'fuse28_infohead_v2_ssd',
    cache               = '/mnt/ssd2t/work/e2e_calib/cache/ns_v3_notile',
    epochs              = 200,
    eval_every          = 10,
    batch_size          = 4,
    img_size            = 256,
    lr                  = 0.0003,
    lr_min              = 1e-06,
    oversample          = 28,
    rot_deg             = 0.5,
    t_m                 = 0.2,
    radial_pert_px      = 0.0,
    min_crop_px         = 256,
    max_crop_px         = 256,
    grid_n              = 16,
    workers             = 4,
    ba_loss             = True,
    ba_iter             = 4,
    ba_damping          = 0.001,
    ba_w_source         = 'infohead',
    ba_warmup_start     = 0,
    ba_warmup_end       = 40,
    ba_weight           = 0.05,
    ba_loss_type        = 'nll',
    grid_iw             = 1600,
    grid_ih             = 900,
    grid_frac           = 0.0,
    crop_grid           = True,
    share_pert          = True,
    ba_detach_mu        = False,
    prefetch            = 4,
    n_iter              = 4,
    n_heads             = 4,
    kv_schedule         = '',
    fourier_head_n_freq = 0,
    fourier_head_scale  = 10.0,
    val_fraction        = 0.1,
    split_seed          = 42,
    use_info_head       = True,
    u_band              = '',
    frame_stride        = '',
    per_cache_oversample= '',
    per_cache_mode      = '',
    pair_stride         = 10,
    pair_mode           = False,
    pair_bidir          = False,
    point_mlp_fourier_n_freq= 0,
    calib_pert          = False,
    w_calib             = 1.0,
    w_pose              = 1.0,
    clearml             = True,
    clearml_project     = 'e2e_calib/calib',
    resume_ckpt         = '/home/hiro/git/e2e_calib/experiments/fuse28_infohead_v2_ssd/last_model.pt',
    start_epoch         = 182,
    why                 = 'Tail resume of fuse28_infohead_v2_ssd: the 17:02:06 host global OOM (swap 8G exhausted by the resident gpu_server/gpu_cluster workers, not a power cut) killed it right after ep182/200. Resuming from last_model.pt (end of ep182) with --start-epoch 182 so the stateless cosine schedule picks up at lr 7.24e-6 and decays to 1e-6 over the remaining 18 ep; Adam moments are NOT restored, so expect a small transient in the first epochs. Everything else identical to the ep0-182 run: base tilepert_ba_from_repro256b, ns_v3_notile cache (nuScenes mini, CAM_FRONT, stride 1, no tile; train=363 val=41, 404 frames, 10 scenes), sigma trained by gaussian2d_nll only, InfoHead2x2 as the GN weight (--ba-w-source infohead, so make_info_from_sigma_rho is never called and no BA gradient reaches sigma), 28-window whole-image tiling fused into ONE GN, perturbation redrawn every __getitem__, ba_weight 0.05 fully ramped (warmup ended at ep40). Prior run reached rollout BA rot=0.0246deg t=0.0045m chi2r=0.9 sigma=1.702px at ep170 (best val_nll 0.8515) and rot=0.0276deg t=0.0074m at ep180; ep170 best is preserved as best_model_ep170_valnll0.8515.pt because best_val resets to inf on resume and ep190/ep200 will overwrite best_model.pt regardless of whether they beat 0.8515.',
)
