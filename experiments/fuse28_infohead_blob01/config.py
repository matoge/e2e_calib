# auto-generated by train_cnd2_ddp.py at run start
CFG = dict(
    name                = 'fuse28_infohead_blob01',
    cache               = '/mnt/ssd2t/work/e2e_calib/cache/ns_v3_notile_blob01',
    epochs              = 50,
    eval_every          = 5,
    batch_size          = 4,
    img_size            = 256,
    lr                  = 0.0003,
    lr_min              = 1e-06,
    oversample          = 28,
    rot_deg             = 0.5,
    t_m                 = 0.2,
    radial_pert_px      = 0.0,
    min_crop_px         = 256,
    max_crop_px         = 256,
    grid_n              = 16,
    workers             = 4,
    ba_loss             = True,
    ba_iter             = 4,
    ba_damping          = 0.001,
    ba_w_source         = 'infohead',
    ba_warmup_start     = 0,
    ba_warmup_end       = 10,
    ba_weight           = 0.05,
    ba_loss_type        = 'nll',
    grid_iw             = 1600,
    grid_ih             = 900,
    grid_frac           = 0.0,
    crop_grid           = True,
    share_pert          = True,
    ba_detach_mu        = False,
    prefetch            = 4,
    n_iter              = 4,
    n_heads             = 4,
    kv_schedule         = '',
    fourier_head_n_freq = 0,
    fourier_head_scale  = 10.0,
    val_fraction        = 0.1,
    split_seed          = 42,
    use_info_head       = True,
    u_band              = '',
    frame_stride        = '',
    per_cache_oversample= '',
    per_cache_mode      = '',
    pair_stride         = 10,
    pair_mode           = False,
    pair_bidir          = False,
    point_mlp_fourier_n_freq= 0,
    calib_pert          = False,
    w_calib             = 1.0,
    w_pose              = 1.0,
    clearml             = True,
    clearml_project     = 'e2e_calib/calib',
    resume_ckpt         = '/mnt/ssd2t/work/e2e_calib/ckpt/tilepert_ba_from_repro256b_best.pt',
    start_epoch         = 0,
    why                 = "fuse28_infohead on 8x data. Same recipe and same base checkpoint (tilepert_ba_from_repro256b) as fuse28_infohead_v2_ssd, only the cache changes: nuScenes mini (10 scenes, train 363 / val 41) -> trainval blob01 (85 scenes, train 2899 / val 477), built by build_ns_cache_blob01.sh with identical --cams CAM_FRONT --stride 1 and no --tile. Motivation: on mini, val_nll rose monotonically 0.8515 (ep170) -> 0.9095 (ep180) -> 0.9595 (ep190) -> 1.0188 (ep200) and chi2r rose 0.9 -> 1.4 while lr was already down at 3e-6, i.e. the InfoHead covariance was drifting over-confident. That is the signature of overfitting 363 frames, so the test is whether 8x data removes it. Point estimates on mini were rot 0.0246-0.0328 deg / t 0.0045-0.0074 m across the four evals, non-monotonic and within eval noise. Epochs cut 200 -> 50 to keep gradient steps comparable (mini 200ep = 18150 steps; blob01 50ep = 36250 steps, ~2x) and BA warmup scaled 40ep -> 10ep for the same reason (7250 steps vs mini's 3630). Note the blob01 val split is 12 scenes chosen by seeded shuffle, scene-level, so no frame leakage; mini and blob01 overlap in at most scene-0061 since blob01 covers scene-0001..0102.",
)
