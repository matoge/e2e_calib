CFG = dict(
    name         = 'ps_v22_live',
    n_layers     = 2,
    img_size     = 64,
    in_channels  = 3,
    use_convnext = False,
    use_frustum  = True,
    use_pose_emb = False,
    use_lidar_kv = False,
    epochs       = 100,
    batch_size   = 64,
    lr           = 0.001,
    lr_min       = 1e-06,
    val_fraction = 0.15,
    split_seed   = 42,
    cameras      = 'front_camera',
    sigma_ypr    = 0.5,
    sigma_t      = 0.2,
    bbox_scale   = 3.0,
    scenes_root  = '/mnt/mininas/datasets/pandaset',
)
