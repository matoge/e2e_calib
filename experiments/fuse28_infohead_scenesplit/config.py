# auto-generated by train_cnd2_ddp.py at run start
CFG = dict(
    name                = 'fuse28_infohead_scenesplit',
    cache               = '/mnt/ssd2t/work/e2e_calib/cache/ns_v3_notile_blob01',
    epochs              = 50,
    eval_every          = 5,
    batch_size          = 4,
    img_size            = 256,
    lr                  = 0.0003,
    lr_min              = 1e-06,
    oversample          = 28,
    rot_deg             = 0.5,
    t_m                 = 0.2,
    radial_pert_px      = 0.0,
    min_crop_px         = 256,
    max_crop_px         = 256,
    grid_n              = 16,
    workers             = 4,
    ba_loss             = True,
    ba_iter             = 4,
    ba_damping          = 0.001,
    ba_w_source         = 'infohead',
    ba_warmup_start     = 0,
    ba_warmup_end       = 10,
    ba_weight           = 0.05,
    ba_loss_type        = 'nll',
    grid_iw             = 1600,
    grid_ih             = 900,
    grid_frac           = 0.0,
    crop_grid           = True,
    share_pert          = True,
    ba_detach_mu        = False,
    prefetch            = 4,
    n_iter              = 4,
    n_heads             = 4,
    kv_schedule         = '',
    fourier_head_n_freq = 0,
    fourier_head_scale  = 10.0,
    val_fraction        = 0.1,
    scene_split         = True,
    split_seed          = 42,
    use_info_head       = True,
    u_band              = '',
    frame_stride        = '',
    per_cache_oversample= '',
    per_cache_mode      = '',
    pair_stride         = 10,
    pair_mode           = False,
    pair_bidir          = False,
    point_mlp_fourier_n_freq= 0,
    calib_pert          = False,
    w_calib             = 1.0,
    w_pose              = 1.0,
    clearml             = True,
    clearml_project     = 'e2e_calib/calib',
    resume_ckpt         = '/mnt/ssd2t/work/e2e_calib/ckpt/tilepert_ba_from_repro256b_best.pt',
    start_epoch         = 0,
    why                 = "Same as fuse28_infohead_blob01 but with --scene-split, so train and val share NO scene (system test S16: train 2899 frames / 73 scenes, val 477 / 12, shared 0; val scene indices 19 26 30 39 49 58 59 62 66 68 73 80). Until now the trainer concatenated the cache's train and val sides and reshuffled at FRAME level (--val-fraction 0.1 -> 'frame-level split: train=3038 val=337'), throwing away the scene split build_nuscenes_v3 had already made. Note the leak was NOT what hurt: evaluating fuse28_infohead_blob01/best_model.pt directly on the 12 held-out scenes gives rot 0.0249 deg / t 0.0046 m / chi2 0.9 / sigma 1.441 px / point 3.285 px over 60 frames, BETTER than its mixed-val figures (0.0285 / 0.0053 / 1.1 / 1.831 / 3.790). Worst held-out frames: rot p90 0.0566 max 0.1988 deg, t p90 0.0128 max 0.0193 m. This run trains on the clean split so train and val are finally the same question. Base tilepert_ba_from_repro256b_best.pt, sigma trained by gaussian2d_nll only, InfoHead is the GN covariance (--ba-w-source infohead), 28-window whole-image fusion, perturbation redrawn every __getitem__, ba_weight 0.05 ramped over ep0-10. System test S1-S16 PASS.",
)
