# auto-generated by train_cnd2_ddp.py at run start
CFG = dict(
    name                = 'cam6_250x2_100ep',
    cache               = '/mnt/ssd2t/work/e2e_calib/cache/ns_250x2_6cam',
    epochs              = 100,
    eval_every          = 5,
    batch_size          = 4,
    img_size            = 256,
    lr                  = 0.0003,
    lr_min              = 1e-06,
    oversample          = 28,
    rot_deg             = 0.5,
    t_m                 = 0.2,
    radial_pert_px      = 0.0,
    min_crop_px         = 256,
    max_crop_px         = 256,
    grid_n              = 16,
    workers             = 4,
    ba_loss             = True,
    ba_iter             = 4,
    ba_damping          = 0.001,
    ba_w_source         = 'infohead',
    ba_warmup_start     = 0,
    ba_warmup_end       = 10,
    ba_weight           = 0.05,
    ba_loss_type        = 'nll',
    grid_iw             = 1600,
    grid_ih             = 900,
    grid_frac           = 0.0,
    crop_grid           = True,
    share_pert          = True,
    ba_detach_mu        = False,
    prefetch            = 4,
    n_iter              = 4,
    n_heads             = 4,
    kv_schedule         = '',
    fourier_head_n_freq = 0,
    fourier_head_scale  = 10.0,
    val_fraction        = 0.1,
    scene_split         = True,
    split_seed          = 42,
    use_info_head       = True,
    u_band              = '',
    frame_stride        = '',
    per_cache_oversample= '',
    per_cache_mode      = '',
    pair_stride         = 10,
    pair_mode           = False,
    pair_bidir          = False,
    point_mlp_fourier_n_freq= 0,
    calib_pert          = False,
    w_calib             = 1.0,
    w_pose              = 1.0,
    clearml             = True,
    clearml_project     = 'e2e_calib/calib',
    resume_ckpt         = '/home/hiro/git/e2e_calib/experiments/front_670x3/best_model.pt',
    start_epoch         = 0,
    why                 = 'All six cameras, 250 scenes x 2 frames x 6 cams = 3000 frames, 100 epochs. Extended from 50 because the previous 6-camera run (fuse28_6cam_300sc) was still improving at ep65 (ep50 0.0586 -> ep60 0.0532 -> ep65 0.0474 deg, point 5.73 -> 5.50 -> 5.28 px) when it was stopped; 50 epochs would cut the cosine short. Base is front_670x3 (603 scenes CAM_FRONT, val 67 scenes with night and rain: rot 0.0244 deg / t 5.7 mm). Applying that front-only model to all six cameras gives CAM_FRONT_RIGHT 0.0229, CAM_BACK_LEFT 0.0268, CAM_FRONT 0.0320, CAM_BACK 0.0381, CAM_FRONT_LEFT 0.0654, CAM_BACK_RIGHT 0.0670 deg -- two cameras it has never seen beat the one it trained on, and four of six land in 0.023-0.038. The local pattern matching transfers across cameras; K goes into the GN so the network never needs to know the intrinsics. The two bad cameras show no camera-specific systematic error even at 240 frames each: the ty bias is negative on all six (-11.2 to -84.9 mm), does not split front/rear, and ty is the least observable DOF (sd 4x the others), so it is model bias rather than calibration. Training on six cameras previously evened them out to 0.042-0.054 deg, improving the bad ones and costing the good ones. 50 epochs is enough (73-scene run: ep40 0.0304 -> ep50 0.0256; 80-epoch run peaked at ep75 0.0244). sigma trained by gaussian2d_nll only, InfoHead is the GN covariance. System test S1-S16 PASS.',
)
