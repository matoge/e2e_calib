CFG = dict(
    name         = 'ps_multicam_corr_clspose_fxfy_100ep',
    n_layers     = 4,
    img_size     = 128,
    in_channels  = 3,
    use_convnext = True,
    use_frustum  = True,
    epochs       = 100,
    batch_size   = 128,
    lr           = 0.0003,
    lr_min       = 1e-07,
    val_fraction = 0.1,
    split_seed   = 42,
    min_crop_px  = 128,
    max_crop_px  = 384,
    cache        = ['/mnt/nvme6t/e2e_calib_cache/pandaset_v3_tiled_multicam_corr'],
    max_rot_deg  = 1.5,
    max_offset_m = 0.6,
    oversample   = 1,
    num_workers  = 12,
    deform_mode  = 'sl',
    use_frame_pose= True,
    frame_pose_weight= 0.5,
    max_fx_pct   = 0.02,
    max_fy_pct   = 0.02,
    frame_pose_dof= 8,
    val_size     = 8000,
)
