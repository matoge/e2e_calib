# auto-generated by train_cnd2_ddp.py at run start
CFG = dict(
    name                = 'front_670x3',
    cache               = '/mnt/ssd2t/work/e2e_calib/cache/ns_670x3',
    epochs              = 80,
    eval_every          = 5,
    batch_size          = 4,
    img_size            = 256,
    lr                  = 0.0003,
    lr_min              = 1e-06,
    oversample          = 28,
    rot_deg             = 0.5,
    t_m                 = 0.2,
    radial_pert_px      = 0.0,
    min_crop_px         = 256,
    max_crop_px         = 256,
    grid_n              = 16,
    workers             = 4,
    ba_loss             = True,
    ba_iter             = 4,
    ba_damping          = 0.001,
    ba_w_source         = 'infohead',
    ba_warmup_start     = 0,
    ba_warmup_end       = 10,
    ba_weight           = 0.05,
    ba_loss_type        = 'nll',
    grid_iw             = 1600,
    grid_ih             = 900,
    grid_frac           = 0.0,
    crop_grid           = True,
    share_pert          = True,
    ba_detach_mu        = False,
    prefetch            = 4,
    n_iter              = 4,
    n_heads             = 4,
    kv_schedule         = '',
    fourier_head_n_freq = 0,
    fourier_head_scale  = 10.0,
    val_fraction        = 0.1,
    scene_split         = True,
    split_seed          = 42,
    use_info_head       = True,
    u_band              = '',
    frame_stride        = '',
    per_cache_oversample= '',
    per_cache_mode      = '',
    pair_stride         = 10,
    pair_mode           = False,
    pair_bidir          = False,
    point_mlp_fourier_n_freq= 0,
    calib_pert          = False,
    w_calib             = 1.0,
    w_pose              = 1.0,
    clearml             = True,
    clearml_project     = 'e2e_calib/calib',
    resume_ckpt         = '/mnt/ssd2t/work/e2e_calib/experiments/fuse28_infohead_scenesplit/best_model.pt',
    start_epoch         = 0,
    why                 = 'CAM_FRONT only, 670 scenes x 3 frames = 2009 frames, 80 epochs. Trains the front camera well enough to then apply the weights to the REAR cameras and look for a bias the model cannot have absorbed. Two earlier attempts failed to separate calibration error from under-training: mini (10 scenes) left sd 54-123 mm so a few mm of rear bias was buried, and its ty bias of -25 to -102 mm appeared on CAM_FRONT too (-30 mm), making it model bias; after 6-camera training the rear was learned and the bias flipped to +7.5 mm, i.e. absorbed. Frame count cut from 3399 (850x4) because scene count, not frame count, is what matters: 10 scenes vs 85 gave the same held-out median (0.0308 vs 0.0241 deg) and differed only in the tail (rot max 1.0248 vs 0.1318 deg). The tail turns out to be weather: on the 850-scene val, clear daytime gives rot 0.0256 deg / t 4.1 mm / point 3.16 px against rain 0.0394 / 7.3 / 6.57 and night 0.0329 / 7.5 / 4.10, with chi2/6 at 0.87-0.96 in every condition, so the model correctly reports its own degradation. Base fuse28_infohead_scenesplit. sigma trained by gaussian2d_nll only, InfoHead is the GN covariance. System test S1-S16 PASS.',
)
