CFG = dict(
    name         = '_smoke_info_head',
    n_layers     = 2,
    img_size     = 256,
    in_channels  = 3,
    use_convnext = True,
    use_frustum  = True,
    epochs       = 1,
    batch_size   = 4,
    lr           = 0.001,
    lr_min       = 1e-06,
    val_fraction = 0.1,
    split_seed   = 42,
    min_crop_px  = 128,
    max_crop_px  = 384,
    cache        = '/home/hfunaya/cache_v4/kamikado_v3_tiled',
    max_rot_deg  = 1.5,
    max_offset_m = 0.2,
    grid_n       = 32,
    num_workers  = 0,
    deform_mode  = 'sl',
    train_size   = 32,
    val_size     = 16,
    use_pose_emb = True,
    use_info_head= True,
    ba_eval      = False,
)
