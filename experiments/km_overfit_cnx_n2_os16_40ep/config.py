CFG = dict(
    name         = 'km_overfit_cnx_n2_os16_40ep',
    n_layers     = 2,
    img_size     = 128,
    in_channels  = 3,
    use_convnext = True,
    use_frustum  = True,
    epochs       = 40,
    batch_size   = 128,
    lr           = 0.001,
    lr_min       = 1e-06,
    val_fraction = 0.1,
    split_seed   = 42,
    min_crop_px  = 256,
    max_crop_px  = 384,
    cache        = '/cache/kamikado_v3_tiled',
    oversample   = 16,
    num_workers  = 8,
    deform_mode  = 'sl',
    val_size     = 800,
)
