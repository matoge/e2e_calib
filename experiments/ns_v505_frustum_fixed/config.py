CFG = dict(
    name         = 'ns_v505_frustum_fixed',
    n_layers     = 4,
    img_size     = 64,
    in_channels  = 3,
    use_convnext = True,
    use_frustum  = True,
    epochs       = 50,
    batch_size   = 128,
    lr           = 0.0003,
    lr_min       = 1e-07,
    val_fraction = 0.1,
    split_seed   = 42,
    min_crop_px  = 128,
    max_crop_px  = 384,
    cache        = '/dev/shm/nuscenes_v3_full',
    oversample   = 1,
    num_workers  = 24,
)
