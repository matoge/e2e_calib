Usage
Run the script:

$ ./hoge.py
The model will be automatically downloaded from ClearML, and it will predict the corrected positions of the points in the image.

Data
The required data is located under:
<sample_script_with_clearml/data>

If the data is not present, download it using Git LFS:
$ git lfs pull